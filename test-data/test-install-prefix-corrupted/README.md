# Test prefix with corrupted Python packages

This folder contains a Python install prefix with "corrupted" data. These
are things like `.dist-info` folders with missing or `METADATA` files that
cannot be parsed.
